/**
 *
 * Space
 *
 */

import * as Antd from 'antd';
import styled from 'styled-components';

const Space = styled(Antd.Space)`
  display: flex;
  justify-content: flex-end;
  margin-top: 20px;
`;

export default Space;
